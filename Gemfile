source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in metabaseIntegration.gemspec
gemspec

#RFC 7519 OAuth JSON Web Token (JWT)
gem 'jwt'